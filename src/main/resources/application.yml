spring:
  application:
    name: demo
  # 로컬 전용 설정 파일이 있으면 기본 설정 위에 덮어쓴다.
  config:
    import: optional:classpath:application-local.yml
  data:
    redis:
      host: localhost
      port: 6379

# JWT
jwt:
  secret: ${JWT_SECRET_KEY}
  expiration: 3600000
