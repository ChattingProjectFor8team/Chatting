global:
  scrape_interval: 15s # 15초마다 데이터를 가져옵니다.

scrape_configs:
  - job_name: 'chatting-app'
    metrics_path: '/actuator/prometheus'
    static_configs:
      # EC2 내부에서 실행 중이라면 public IP나 사설 IP를 적어주세요.
      - targets: ['3.36.88.138:8080']