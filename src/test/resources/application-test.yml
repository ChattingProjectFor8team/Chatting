spring:
  datasource:
    url: jdbc:mysql://localhost:33306/springchatting_test?createDatabaseIfNotExist=true&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: root_password
  data:
    redis:
      host: localhost
      port: 6380
  jpa:
    hibernate:
      ddl-auto: create-drop

media:
  storage:
    enabled: false
